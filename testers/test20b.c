// 简单的C程序示例
void main() {
    int a = 10;
    int b = 20;
    int sum = a + b, i, t = 0;

    // 条件语句示例
    if (sum > 25) {
        print("和大于25\n");
    } else {
        print("和小于等于25\n");
    }
    
    // 循环示例
    for (i = 0; i < sum; i++) {
        i += i;
    }
    print(i);
    
}